# ============================================================
# 博客容器内部 Caddy 配置：仅提供 HTTP 静态服务（容器内 :80）
# 对外 HTTPS 由 VPS 主机上的 Caddy 反向代理到本容器。
# ============================================================
:80 {
	root * /srv
	encode gzip
	file_server
	try_files {path} {path}/ /404.html
}
